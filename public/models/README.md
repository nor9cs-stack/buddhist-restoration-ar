# Model Placeholder

Place the optimized production model at:

```text
public/models/buddha_001.glb
```

The app intentionally keeps `/models/buddha_001.glb` as the runtime URL in `src/data/statues.ts`. The GLB file itself is ignored by Git while the large source model is being optimized.
